# Web framework
fastapi
uvicorn[standard]
python-multipart

# Vector database
qdrant-client

# Embeddings
# sentence-transformers is still needed for the CrossEncoder re-ranker fallback.
# Embeddings are API-based: Voyage AI (voyage-code-3) or Nomic (nomic-embed-text-v1.5).
sentence-transformers
voyageai         # Voyage AI embeddings (voyage-code-3, 1024-dim, code-optimised)
cohere           # Cohere Rerank API (rerank-v3.5) — falls back to local cross-encoder

# Repo fetching
gitpython
requests

# Code parsing
tree-sitter          # multi-language AST parsing
tree-sitter-python   # Python grammar for tree-sitter

# LLM providers
groq
anthropic
openai          # OpenAI-compatible API calls (Gemini + OpenRouter both use this SDK)

# Utilities
python-dotenv
pydantic
mcp
posthog

# Semantic Map — 2D projection of code embeddings
scikit-learn  # PCA fallback + K-means clustering
umap-learn    # UMAP projection (better clusters than PCA; PCA used if not installed)
